(function(w){
  w.eruda = w.eruda || {
    init: function(){ console.info('Local eruda placeholder loaded.'); },
    show: function(){ console.info('DevTools placeholder: add full eruda bundle to vendor/eruda.js'); }
  };
})(window);
